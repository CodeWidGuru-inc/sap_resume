name: Build and Deploy SAPUI5 App to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write  # Required to push to gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install SAPUI5 tooling
        run: npm install --global @ui5/cli

      - name: Install project dependencies
        run: npm install

      - name: Build SAPUI5 project
        run: ui5 build --all

      - name: Copy CNAME file to dist
        run: cp CNAME dist/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          cname: guruteja.chilukoti.sap.unaux.com